<!DOCTYPE html>
<html ng-app="app">
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
    <link rel='stylesheet' href='/css/style.css' />
    <script src='/js/jquery-2.0.3.js'></script>
    <script src='/js/bootstrap.js'></script>
    <script src='/js/angular.min.js'></script>
    <script src='/js/angular-sanitize.min.js'></script>
    <script src="/bower_components/Chart.js/Chart.js"></script>
    <script src="/bower_components/angular-chart.js/dist/angular-chart.js"></script>
    <script src='/js/app.js'></script>
  </head>
  <body ng-controller="MainCtrl">

    <% include ../views/nav %>

    <div class="container">
      <h1><%= title %></h1>
      <% if (user) { %>
      <div class="bg-warning pad20">
        <p class="font16">Hello, <%= user.name %>!</p>
        <p> 歡迎來到 <%= title %> 以下說明很重要，請仔細閱讀，若有問題請聯絡 evin92@gmail.com</p>
        <p>緊急時刻請打 0987-838-646 林哲緯</p>
      </div>
      <div class="bg-success pad20">此次共109人報名</div>
      <div class="bg-info intro font16">
        <p>點擊單一列後方「批改」鈕即會進入該名學生批改頁面</p>
        <p>評分完後按下送出會回到此頁（即為首頁）</p>
      </div>
      <div class="bg-danger pad20">
        <p>使用完後請記得  <a href="/logout">登出</a> （下次瀏覽器會記住登入狀態）</p>
      </div>
      <hr>
      <h2>報名列表</h2>
      <div class="filter form-inline">快速篩選:
      <input type="text" ng-model="id_filter" class="form-control" placeholder="輸入數字"> 或 
      <input type="text" ng-model="name_filter" class="form-control" placeholder="輸入名字">
      </div>
      <table class="table table-hover header-fixed">
        <thead>
          <tr>
            <th>#</th>
            <th>name</th>
            <th>student id</th>
            <th>college</th>
            <th>department</th>
            <th>grade</th>
            <th>~</th>
          </tr>
        </thead>
        <tbody>
          <tr ng-repeat="user in users | orderBy:'id' | filter: {'id': id_filter, 'name': name_filter}" ng-show="user_id == '1'">
            <td>{{user.id}}</th>
            <td>{{user.name}}</td>
            <td>{{user.student_id}}</td>
            <td>{{user.college}}</td>
            <td>{{user.department}}</td>
            <td>{{user.grade}}</td>
            <td ng-show="user.score.skill_score && user.score.cover_link_score" class="text-success"><span class="glyphicon glyphicon-ok" aria-hidden="true"></span> 已批改 | <a class="text-danger" href="/applicants/{{user.id}}">修改</a></td>
            <td ng-hide="user.score.skill_score && user.score.cover_link_score"><a href="/applicants/{{user.id}}">批改</a></td>
          </tr>
          <tr ng-repeat="user in users | orderBy:'id' | filter: {'id': id_filter, 'name': name_filter}" ng-show="user_id == '2'">
            <td>{{user.id}}</th>
            <td>{{user.name}}</td>
            <td>{{user.student_id}}</td>
            <td>{{user.college}}</td>
            <td>{{user.department}}</td>
            <td>{{user.grade}}</td>
            <td ng-show="user.score.youtube_link_score" class="text-success"><span class="glyphicon glyphicon-ok" aria-hidden="true"></span> 已批改 | <a class="text-danger" href="/applicants/{{user.id}}">修改</a></td>
            <td ng-hide="user.score.youtube_link_score"><a href="/applicants/{{user.id}}">批改</a></td>
          </tr>
          <tr ng-repeat="user in users | orderBy:'id' | filter: {'id': id_filter, 'name': name_filter}" ng-show="user_id == '3'">
            <td>{{user.id}}</th>
            <td>{{user.name}}</td>
            <td>{{user.student_id}}</td>
            <td>{{user.college}}</td>
            <td>{{user.department}}</td>
            <td>{{user.grade}}</td>
            <td ng-show="user.score.success_item_score && user.score.fail_item_score" class="text-success"><span class="glyphicon glyphicon-ok" aria-hidden="true"></span> 已批改 | <a class="text-danger" href="/applicants/{{user.id}}">修改</a></td>
            <td ng-hide="user.score.success_item_score && user.score.fail_item_score"><a href="/applicants/{{user.id}}">批改</a></td>
          </tr>
        </tbody>
      </table>

      <% } else { %>
      <p>請先登入！</p>
      <% } %>
    </div> <!-- end of container -->
  </body>
</html>
